<!doctype html>
<html lang="fr" ng-app="<%= main %>" ng-strict-di id="ng-app">
    <head>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Raphaël Yan</title>
        <link rel="shortcut icon" type="image/x-icon" href="mocks/favicon.ico" />
        <script type="text/javascript">
            var _gaq = _gaq || [];
            _gaq.push(['_setAccount', 'UA-52202995-3']);

            (function() {
                var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
                ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
                var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
            })();
        </script>
<% if (base) {
%>        <base href="<%= base %>">
<% } %><% styles.forEach(function(href) {
%>        <link rel="stylesheet" href="<%= href %>">
<% }) %>
<% scripts.forEach(function(src) {
%>        <script src="<%= src %>"></script>
<% }) %>    </head>
    <body>
        <div class="desktop-only" ui-view></div>
        <div class="mobile-only">Mobiles/Tablets<br>are not supported yet<br>on this website.</div>
    </body>
</html>
